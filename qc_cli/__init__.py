"""Shared infrastructure for the qc-tools command line tools.

Contains the QingCloud API building blocks shared by every CLI in the
suite (AppCenter, IaaS, ...): request signing, the HTTP client, config
loading, output rendering, and the generic argparse-based CLI framework.
"""

__version__ = "0.1.0"
